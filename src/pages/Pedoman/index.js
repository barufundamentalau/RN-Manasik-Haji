import React from 'react';
import {StyleSheet, Text, View} from 'react-native';
import {HeaderMenu} from '../../components';

const Pedoman = () => {
  return (
    <View>
      <HeaderMenu title="Pedoman" />
      <Text>Pedoman Page</Text>
    </View>
  );
};

export default Pedoman;

const styles = StyleSheet.create({});
